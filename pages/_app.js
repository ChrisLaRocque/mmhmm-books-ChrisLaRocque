import '../styles/globals.scss' // Generic styles from create-next-app and a few helper styles for general layout

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
